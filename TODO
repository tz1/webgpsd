prlock.kml formatting error (add2kml doesn't appear to rewind).
add lock to satstats
DOCUMENT CHUMBY IN README, update for files

Chumby: LEDS

/tmp/laststate - save gps for warm start, not 0,0

Add option to set clocks on chumby (code there but inactive)

Harley:
	add status to main if active
	j1850 watchdog in hogdev (?)
	Sleep webgpsd (suspend logging to current.kml or if short timeout do a rotatelog) if no J1850 data.

Config / comline:
       /etc/webgpsd
       multi listen ports?
       follow comline?

Sources:
       serial port set comm via termios?

Web:
       json array of sources?
       (need something like this for gpsd-ng)

GPSD:
        move to json commands (?), sync format with gpsd-ng.

