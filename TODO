Config / comline:
	where to store?
        Listen ports as array, list in cmdline?
        chdir for logfiles
        err log name/loc?

Sources:
        python connect/send script (looped?) launcher
        serial port set comm via termios?

Web:
        per-source status display?
        return array of sources?
	(need something like this for gpsd-ng)

GPSD:
        move to json commands (?)

Harley/OBD:
        reintegrate or split to harleyd
        revive miniobdd
